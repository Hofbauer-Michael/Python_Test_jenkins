pipeline {
    agent any

    stages {
        stage('Run Pytest') {
            steps {
                echo 'Hello World'
                echo 'Hello World1'
            }

    stages {
        stage('Installing Pytest') {
            steps {
                echo 'Install Pytest'
                bat 'test.py'
            }

            
        }
    }
}
